<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SuitmediaMobileTest" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/main</item>
    </style>

    <style name="Base.Theme.SuitmediaMobileTextWithActionBar" parent="Theme.Material3.DayNight">
        <item name="colorPrimary">@color/main</item>
    </style>
    <style name="Theme.SuitmediaMobileTest" parent="Base.Theme.SuitmediaMobileTest">
    </style>
</resources>